// ============================================
// Configuration — API Keys & Secrets
// ============================================
// Copy this file to config.js and replace with your actual keys.
// config.js is gitignored and will NOT be committed to the repository.
//
// For Cloudflare Pages deployment:
//   Set GOOGLE_MAPS_API_KEY as an environment variable in Cloudflare Pages settings.
//   The build command (node build.js) will auto-generate config.js from the env var.

window.BIBLIA_CONFIG = {
    GOOGLE_MAPS_API_KEY: 'YOUR_API_KEY_HERE'
};
